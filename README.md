1. Собрать контейнеры ```docker-compose build```
2. Поднять контейнеры  ```docker-compose up -d```
3. Композер ```docker-compose run --rm -u root app composer i```
4. Миграции ```docker-compose run --rm app php artisan migrate```
